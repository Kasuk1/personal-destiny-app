import { HeroStyles } from './HeroSectionStyles';

export const HeroSection = () => (
  <HeroStyles>
    <div className="hero">
      <h1 className="hero--title">
        The easiest way to find your next destination.
      </h1>
    </div>
  </HeroStyles>
);
